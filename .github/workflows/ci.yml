# Name of the workflow that appears in GitHub Actions
name: CI

# Workflow version: 1.0 - Initial pitaya CI

# Define when this workflow will run
on:
  pull_request:
  push:
    branches: [main, master, develop]
  workflow_dispatch:

jobs:
  # Job 1: Build
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [clang, gcc]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache APT packages
        uses: awalsh128/cache-apt-pkgs-action@v1.4.3
        with:
          packages: cmake ninja-build clang gcc g++ ccache mold llvm cppcheck
          version: 1.0

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ runner.os }}-${{ matrix.compiler }}-ccache
          restore-keys: |
            ${{ runner.os }}-${{ matrix.compiler }}-
            ${{ runner.os }}-
          max-size: 500M
          verbose: 2

      - name: Set compiler variables
        run: |
          if [ "${{ matrix.compiler }}" = "clang" ]; then
            echo "CC=clang" >> $GITHUB_ENV
            echo "CXX=clang++" >> $GITHUB_ENV
          else
            echo "CC=gcc" >> $GITHUB_ENV
            echo "CXX=g++" >> $GITHUB_ENV
          fi

      - name: Configure CMake project
        run: |
          cmake -G Ninja -B build \
                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                -DCMAKE_C_COMPILER=$CC \
                -DCMAKE_CXX_COMPILER=$CXX \
                -DCMAKE_CXX_STANDARD=20 \
                -DCMAKE_CXX_STANDARD_REQUIRED=ON \
                -DPITAYA_BUILD_TESTING=ON \
                -DPITAYA_ENABLE_CCACHE=ON \
                -DPITAYA_ENABLE_CPPCHECK=ON 
          echo "=== CMake Configuration Complete ==="

      - name: Build all test targets
        run: |
          echo "=== Starting Build at $(date) ==="
          time cmake --build build --parallel $(nproc) --target pitaya_tests
          echo "=== Build Complete at $(date) ==="

      - name: Upload test binaries
        uses: actions/upload-artifact@v4
        with:
          name: test-binaries-${{ matrix.compiler }}
          path: |
            build/bin/pitaya_tests
          retention-days: 3
          compression-level: 9

  # Job 2: Unit Tests
  test-unit:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [clang, gcc]
      fail-fast: false
    steps:
      - name: Download test binaries
        uses: actions/download-artifact@v4
        with:
          name: test-binaries-${{ matrix.compiler }}
          path: build/bin/

      - name: Make binaries executable
        run: chmod +x build/bin/*

      - name: Run unit tests
        run: |
          echo "=== Running Unit Tests with ${{ matrix.compiler }} ==="
          ./build/bin/pitaya_tests --gtest_color=yes

  # Job 3: Documentation
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache APT packages
        uses: awalsh128/cache-apt-pkgs-action@v1.4.3
        with:
          packages: doxygen graphviz
          version: 1.0

      - name: Generate Documentation
        run: doxygen Doxyfile

      - name: Upload Documentation
        uses: actions/upload-artifact@v4
        with:
          name: pitaya-docs
          path: docs/html
          retention-days: 7

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GTEST_COLOR: 1
